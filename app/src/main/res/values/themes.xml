<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SequeniaTest" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="actionBarTheme">@style/ActionBarCustom</item>
    </style>

    <style name="Theme.SequeniaTest" parent="Base.Theme.SequeniaTest" />

    <style name="ActionBarCustom" parent="Base.Widget.AppCompat.ActionBar">
        <item name="titleCentered">true</item>
        <item name="android:background">@color/blue</item>
        <item name="titleTextColor">@color/white</item>
        <item name="elevation">4dp</item>


    </style>

</resources>